spring:
  application:
    name: apiclaro

  datasource:
    url: jdbc:postgresql://localhost:5433/task
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true